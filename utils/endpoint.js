import { apiHost as baseUrl } from '../config'

export const ordersService = `${baseUrl}/orders`;
export const bankService = `${baseUrl}/bank`;
export const userService = `${baseUrl}/users`;
export const withdrawService = `${baseUrl}/withdraw`;
